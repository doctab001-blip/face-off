# Required for fal.ai inpainting via /api/fal/proxy
FAL_KEY="key_id:key_secret"

# Optional — only needed if you re-enable Clerk auth in layout.tsx
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=
